// Indice         0  1  2  3  4     5
const numeros = [ 10,20,30,40,50,[1,2,3]];

console.table(numeros);

// Acceder al arreglo
console.log(numeros[2]);

console.log(numeros[5][1]);     // acceder a arreglo dentro de arreglo