const producto = 'Monitor 20"';

console.log(producto.length);    // Muestra cuantos carracteres tiene el String


console.log(producto.indexOf("20"));  // Buscar si "20" existe en la variable producto nos dice donde esta / -1 = no encontrado

console.log(producto.includes("Monitor"));  // Buscar si "i" existe en la variable producto / false = no encontrado
